apiVersion: v1
kind: Service
metadata:
  name: clinic-frontend
  namespace: clinic
spec:
  selector:
    app: clinic-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.ports.frontend }}